export const NODE_ENV = process.env.NODE_ENV;

export const BASE_URL = process.env.API_URL;

export const API = {
   POSTS: `posts`,
   COMMENTS: `comments`,
   USERS: `users`,
};

export default {
   NODE_ENV,
   BASE_URL,
   API,
};
